# Data Access Policy Check

This example models a scoped data read check.

The foundation verifies actor authority and evaluates policy. It does not read data.

Use action class `demo.read` with resource `urn:dataset/customer-safe-summary`.

